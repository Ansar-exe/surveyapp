# Server configuration
PORT=5000
NODE_ENV=development

# JWT secret key (change this in production!)
JWT_SECRET=surveyPro98_super_secret_key_change_me

# JWT expiration
JWT_EXPIRES_IN=7d

# MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/surveyapp
